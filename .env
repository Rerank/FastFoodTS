# Переменные окружения фронтенда (Vite).
# ВАЖНО: только переменные с префиксом VITE_ попадают в клиентский код,
# и они ПУБЛИЧНЫ (видны в браузере). Секреты сюда класть нельзя.

# Базовый URL backend API
VITE_API_BASE_URL=http://localhost:3000/api
