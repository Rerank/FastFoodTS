# Шаблон переменных окружения фронтенда. Скопируй в .env.
# Только VITE_-переменные доступны в коде; они публичны (без секретов).

# Базовый URL backend API
VITE_API_BASE_URL=http://localhost:3000/api
